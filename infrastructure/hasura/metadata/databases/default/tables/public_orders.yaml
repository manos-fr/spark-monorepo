table:
  name: orders
  schema: public
array_relationships:
  - name: order
    using:
      foreign_key_constraint_on:
        column: order_id
        table:
          name: user_orders
          schema: public
